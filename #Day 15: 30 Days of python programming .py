# 1. SyntaxError - missing closing parenthesis
print("Hello World"

# 2. NameError - using a variable that doesn't exist
print(undefined_variable)

# 3. IndexError - accessing an index that doesn't exist
my_list = [1, 2, 3]
print(my_list[10])

# 4. ModuleNotFoundError - importing a module that doesn't exist
import nonexistent_module